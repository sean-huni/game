---
# Items that apply to ALL profiles:
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.0.5:8001/eureka/

---
spring:
  application:
    name: game
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8002
      name: game-service

server:
  port: 8006
